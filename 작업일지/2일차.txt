2일차

1. 메모 삭제 버튼 오류를 고치지 못하였다.

- setter 를 사용해보라하여 setter를 사용해서 메모가 새로 생성될때마다 NodeList 를 console을 찍어보았다. 나온다.
- 그렇게 생성된것을 확인하고 for문을 한번더 돌렸다 근데 안된다.
- for문에는 removeBtn.length 만큼 반복하여 삭제버튼에 이벤트를 적용하고 있다.
- 확인해보니 removeBtn.length 가 계속 1이다.
- 새로 length 변수를 선언하여 for문에 새롭게 적용했다
- 그런데 여전히 안된다.
- 뭐가 잘못된거지..

2. 문제점

- 동적으로 추가된 요소는 추가된 후 이벤트를 다시 적용해 주어야 한다고 조언받았다.
- innerHTML를 쓰지말고 createElement를 쓰라더라 (차이점이 뭐지)
- setter 함수를 쓰면 동적으로 변경되는 DOM요소를 감지할수 있다 하더라 써보았는데 그런것같다.